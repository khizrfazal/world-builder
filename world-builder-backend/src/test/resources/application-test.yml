spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/worldbuilder_db_test
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true

logging:
  level:
    root: WARN
