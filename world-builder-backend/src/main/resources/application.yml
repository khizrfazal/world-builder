spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: worldbuilder

  flyway:
    enabled: true
    default-schema: worldbuilder
    schemas: worldbuilder

  profiles:
    active: dev